# Google account email used to select the Ramp SSO login identity
# (single value, first non-comment line wins).
# The public repo ships no default. Set the real account in the profile layer:
#   ~/.tango/profiles/<profile>/config/browser/ramp-google-account.txt
# Overrides: TANGO_RAMP_GOOGLE_ACCOUNT_EMAIL (value) or *_FILE (path), then
# TANGO_WORK_GOOGLE_ACCOUNT_EMAIL as a fallback.
