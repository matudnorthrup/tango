version: 1
description: Generic Tango full-stack tmux startup.

tmux:
  session: tango
  socketName: tango-service
  cleanup:
    staleSocket: true
  legacySessions:
    - tango-discord
    - tango-voice
    - kokoro
    - whisper-server
    - whisper-partials
    - owntracks

hooks:
  enabled: true

services:
  - id: kokoro
    name: Kokoro TTS server
    enabled: true
    optional: true
    cwd: "${HOME}/Kokoro-FastAPI"
    command: >-
      .venv/bin/python -m uvicorn api.src.main:app --host 0.0.0.0 --port 8880
    tmux:
      window: kokoro
    retries:
      attempts: 2
      delayMs: 1000
      timeoutMs: 15000
    health:
      checks:
        - type: tmux-window
        - type: tcp
          host: 127.0.0.1
          port: 8880

  - id: whisper-main
    name: Whisper STT server main
    enabled: true
    optional: true
    cwd: "${REPO_DIR}"
    env:
      WHISPER_MODEL: "${HOME}/whisper-models/ggml-small.en.bin"
      WHISPER_LANGUAGE: "en"
      WHISPER_PROMPT: "Tango"
    command: >-
      whisper-server --model "$WHISPER_MODEL" --port 8178 --language "$WHISPER_LANGUAGE" --prompt "$WHISPER_PROMPT"
    tmux:
      window: whisper-main
    retries:
      attempts: 2
      delayMs: 1000
      timeoutMs: 15000
    health:
      checks:
        - type: tmux-window
        - type: tcp
          host: 127.0.0.1
          port: 8178

  - id: whisper-partials
    name: Whisper STT server partials
    enabled: true
    optional: true
    cwd: "${REPO_DIR}"
    env:
      WHISPER_MODEL: "${HOME}/whisper-models/ggml-small.en.bin"
      WHISPER_LANGUAGE: "en"
      WHISPER_PROMPT: "Tango"
    command: >-
      whisper-server --model "$WHISPER_MODEL" --port 8179 --language "$WHISPER_LANGUAGE" --prompt "$WHISPER_PROMPT"
    tmux:
      window: whisper-partials
    retries:
      attempts: 2
      delayMs: 1000
      timeoutMs: 15000
    health:
      checks:
        - type: tmux-window
        - type: tcp
          host: 127.0.0.1
          port: 8179

  - id: owntracks
    name: OwnTracks receiver
    enabled: true
    optional: false
    cwd: "${REPO_DIR}"
    envFiles:
      - path: ".env"
        required: false
    command: >-
      node apps/owntracks-receiver/server.js
    tmux:
      window: owntracks
    retries:
      attempts: 2
      delayMs: 1000
      timeoutMs: 15000
    health:
      checks:
        - type: tmux-window
        - type: tcp
          host: 127.0.0.1
          port: 3456

  - id: discord
    name: Tango Discord runtime
    enabled: true
    optional: false
    cwd: "${REPO_DIR}"
    unsetEnv:
      - CLAUDECODE
    env:
      DISCORD_LISTEN_ONLY: "false"
    command: >-
      "${NODE_BIN}" packages/discord/dist/main.js
    tmux:
      window: discord
    retries:
      attempts: 2
      delayMs: 1000
      timeoutMs: 15000
    health:
      checks:
        - type: tmux-window

  - id: voice
    name: Tango Voice pipeline
    enabled: true
    optional: false
    cwd: "${VOICE_APP_DIR}"
    command: >-
      "${NODE_BIN}" dist/index.js 2>&1 | tee /tmp/tango-voice.log
    tmux:
      window: voice
    retries:
      attempts: 2
      delayMs: 1000
      timeoutMs: 15000
    health:
      checks:
        - type: tmux-window

  - id: remote-control
    name: Remote control watchdog
    enabled: true
    optional: true
    cwd: "${REPO_DIR}"
    command: >-
      scripts/remote-control-watchdog.sh Tango
    tmux:
      window: remote-control
    retries:
      attempts: 2
      delayMs: 1000
      timeoutMs: 15000
    health:
      checks:
        - type: tmux-window

  - id: home
    name: Tailnet home site
    enabled: true
    optional: false
    cwd: "${REPO_DIR}/apps/home"
    command: >-
      "${NODE_BIN}" server.mjs
    tmux:
      window: home
    retries:
      attempts: 2
      delayMs: 1000
      timeoutMs: 15000
    health:
      checks:
        - type: tmux-window
        - type: tcp
          host: 127.0.0.1
          port: 9310

  - id: kilo-ledger
    name: Kilo Ledger
    enabled: true
    optional: false
    cwd: "${REPO_DIR}/apps/kilo-ledger"
    command: >-
      "${NODE_BIN}" dist/server.js
    tmux:
      window: kilo-ledger
    retries:
      attempts: 2
      delayMs: 1000
      timeoutMs: 15000
    health:
      checks:
        - type: tmux-window
        - type: tcp
          host: 127.0.0.1
          port: 9320

  - id: food-ui
    name: Food UI
    enabled: true
    optional: false
    cwd: "${REPO_DIR}/apps/food-ui"
    command: >-
      "${NODE_BIN}" dist/server/main.js
    tmux:
      window: food-ui
    retries:
      attempts: 2
      delayMs: 1000
      timeoutMs: 15000
    health:
      checks:
        - type: tmux-window
        - type: tcp
          host: 127.0.0.1
          port: 9370

  - id: workout-ui
    name: Workout UI
    enabled: true
    optional: false
    cwd: "${REPO_DIR}/apps/workout-ui"
    command: >-
      "${NODE_BIN}" dist/server/main.js
    tmux:
      window: workout-ui
    retries:
      attempts: 2
      delayMs: 1000
      timeoutMs: 15000
    health:
      checks:
        - type: tmux-window
        - type: tcp
          host: 127.0.0.1
          port: 9330
