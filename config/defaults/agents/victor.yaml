id: victor
type: operations
display_name: Victor
provider:
  default: claude-oauth
  reasoning_effort: max
  fallback:
    - claude-oauth-secondary
    - codex
default_project: operations
voice:
  call_signs:
    - Victor
  kokoro_voice: am_liam
  default_channel_id: "100000000000000004"
  smoke_test_channel_id: "100000000000001004"
response_mode: concise
tools:
  mode: off
orchestration:
  worker_ids:
    - operations-assistant
    - note-librarian
  write_confirmation: on-ambiguity
deterministic_routing:
  enabled: true
  confidence_threshold: 0.8
  provider:
    default: claude-oauth
    reasoning_effort: low
    fallback:
      - claude-oauth-secondary
      - codex
prompt_file: ../../../agents/assistants/victor/soul.md
