{
  "name": "@tango/kilo-ledger",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build -w @tango/core && tsx src/server.ts",
    "start": "node dist/server.js",
    "test": "npm run build -w @tango/core && vitest run"
  },
  "dependencies": {
    "@tango/core": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^3.2.6"
  }
}
